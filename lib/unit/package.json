{
  "name": "@morpho/unit",
  "alias": "unit",
  "version": "0.1.3",
  "description": "Adds default units for css properties with numerical values",
  "homepage": "https://github.com/B-Teague/morpho#readme",
  "main": "src/index.js",
  "unpkg": "src/index.js",
  "module": "src/index.js",
  "scripts": {
    "test": "cross-env-shell ../../node_modules/.bin/nyc -i esm -r lcov ../../node_modules/.bin/testmatrix test/unit.test.js && cross-env-shell ../../node_modules/.bin/nyc report",
    "build": "npm run createIndex && npm run bundle && npm run minify",
    "createIndex": "node ./util/createIndex.js",
    "bundle": "cross-env-shell DIR=$PKG PKG=$npm_package_alias ../../node_modules/.bin/rollup -i ${DIR}$npm_package_module -o ${DIR}dist/$PKG.js --no-esModule -mf iife -n $PKG",
    "minify": "cross-env-shell DIR=$PKG PKG=$npm_package_alias ../../node_modules/.bin/terser ${DIR}dist/$PKG.js -o ${DIR}dist/$PKG.js -mc --source-map includeSources,url=$PKG.js.map"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/B-Teague/morpho/tree/master/lib/unit"
  },
  "keywords": [
    "morpho",
    "css-in-js",
    "@morpho/unit",
    "css",
    "hyperapp",
    "react"
  ],
  "author": "Brian Teague",
  "license": "MIT",
  "devDependencies": {
    "cross-env": "^7.0.2"
  }
}
